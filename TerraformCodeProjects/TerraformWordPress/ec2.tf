# NOTE: This project's resources have been moved into ./modules/ec2.tf
# and are now invoked via the module "this" block in main.tf.
# This file is intentionally left empty (kept only because it can't be
# deleted from here) to avoid declaring duplicate resources in the root
# module. It's safe to delete this file from your editor/terminal.
